<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }
      .profile-card {
        border: none;
        border-radius: 1rem;
        overflow: hidden;
      }
      .profile-left {
        background: linear-gradient(to right, #06cab3, #08caf6);
        color: white;
        padding: 2rem;
      }
      .profile-img {
        width: 100px;
        height: 100px;
        object-fit: cover;
        border-radius: 50%;
      }
    </style>
  </head>
  <body>
    <%- include("./pages/navbar") %>

    <div class="container py-5">
      <div class="row justify-content-center">
        <div class="col-md-10">
          <div
            class="card profile-card shadow-lg d-flex flex-md-row flex-column"
          >
            <div
              class="profile-left text-center d-flex flex-column align-items-center justify-content-center"
            >
              <img
                src="<%= data.image %>"
                alt="User"
                class="profile-img mb-3"
              />
              <h4><%= data.name %></h4>
              <p><%= data.title %></p>
              <a href="/api/auth/editprofile" class="btn btn-light btn-sm mt-3"
                >Edit Profile</a
              >
              <a href="/api/auth/logout" class="btn btn-danger btn-sm mt-3"
                >Logout</a
              >
            </div>
            <div class="p-4 w-100">
              <h5 class="border-bottom pb-2 mb-3">Information</h5>
              <div class="row mb-4">
                <div class="col-sm-6">
                  <p class="text-muted mb-1">Email</p>
                  <p><%= data.email %></p>
                </div>
                <div class="col-sm-6">
                  <p class="text-muted mb-1">Phone</p>
                  <p><%= data.phone %></p>
                </div>
              </div>
              <h5 class="border-bottom pb-2 mb-3">Wishlist</h5>
              <div class="row mb-4">
                <div class="col-sm-6">
                  <p class="text-muted mb-1">Product 1</p>
                  <p>Product 1 Photo</p>
                </div>
                <div class="col-sm-6">
                  <p class="text-muted mb-1">Product 2</p>
                  <p>Product 2 Photo</p>
                </div>
              </div>
              <div>
                <a href="/products" class="btn btn-outline-primary"
                  >See All Products</a
                >
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
